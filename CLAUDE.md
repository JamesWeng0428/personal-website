# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
npm run dev       # Start dev server at localhost:4321
npm run build     # Build production site to ./dist/
npm run preview   # Preview production build locally
```

No test suite is configured.

## Architecture

Astro 6 static site deployed to GitHub Pages at `https://JamesWeng0428.github.io`.

**Content model**: Blog posts live in `src/content/blog/` as Markdown/MDX files. The schema (title, description, pubDate, updatedDate?, heroImage?) is defined in `src/content.config.ts`. Site-wide constants (SITE_TITLE, SITE_DESCRIPTION) are in `src/consts.ts`.

**Routing**: File-based via `src/pages/`. Dynamic blog routes use `[...slug].astro`. RSS feed is generated by `src/pages/rss.xml.js`.

**Layouts & Components**: `src/layouts/BlogPost.astro` wraps blog posts. Shared UI is in `src/components/` (BaseHead, Header, Footer, HeaderLink, FormattedDate).

**Styling**: Single `src/styles/global.css` with CSS custom properties for theming. Component-scoped styles are defined inline within `.astro` files.

**Static assets**: `public/` contains fonts, images (cycling photos, blog placeholders), and bouldering MP4 videos served as-is. Local images in `src/assets/` go through Astro's image optimization pipeline (via `sharp`).

**Deployment**: GitHub Actions workflow at `.github/workflows/deploy.yml` — pushes to `main` trigger a build and deploy to GitHub Pages using Node.js 24.
